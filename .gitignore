# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Captures du test de fumée
.smoke/
# Les captures de travail : elles servent à juger un rendu pendant qu'on le
# fabrique, elles n'ont rien à faire dans l'historique. Quatre d'entre elles y
# sont entrées par un `git add -A` distrait, et c'est ce qui vaut cette ligne.
captures/
dist-single/

# Rapports mobiles
.mobile/
.clavier/
.paysage/
.perf/
# L'inventaire de parité du jour. Le témoin, lui, est versionné dans
# `tools/parite-temoin.json` — c'est lui qui sert de référence.
.parite/
.paysage/
.perf/
.perf/

# La toile de conception assemblée : 2,5 Mo d'éditeur empaqueté, régénéré
# depuis les sources `design/*.dc.html` par le skill de design. Ce sont les
# sources qui se versionnent, pas la sortie.
design/**/*.html
!design/**/*.dc.html
